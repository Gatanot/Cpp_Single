#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    // 练习运算符的简单程序
    /*cout << "tnter a floating-point value: ";
    double n;
    cin >> n;
    cout << " n==  " << n
         << "\n n+1 = " << n + 1
         << "\n 3 times n =" << 3 * n
         << "\n n squared =" << n * n
         << "\n half of n =" << n / 2
         << "\n square root of n =" << sqrt(n);
    */

    // read two names
    /*cout << "enter your first name and second name: ";
    string first, second;
    cin >> first >> second;
    string name = first + ' ' + second;
    cout << "hello, " << name << '\n';
    */

    // read and compete two names
    /*cout << "please enter two names";
    string first, second;
    cin>>first>>second;
    if (first == second)
    {
        cout << "that's the same name twice\n";
    }
    else if (first < second)
    {
        cout << first << "is alphabetically before " << second << '\n';
    }
    else if (first > second)
    {
        cout << first << "is alphabetically after " << second << '\n';
    }
    */
   int a =0;
   a = 1;

    return 0;
}