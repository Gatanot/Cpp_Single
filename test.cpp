#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;
int main(void)
{
    vector<int> numlsit;
    for (int i = 0; i < 3; i++)
    {
        cin >> numlsit[1];
    }
    sort(numlsit);
    return 0;
}